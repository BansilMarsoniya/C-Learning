#include <stdio.h>

int main(){
    int i = 10;
    int j = i;
    // %d is called formate specifier
    // %d is for "int", %f is for "float" AND %c is for "char"
    printf("The value of i is %d\n and value of j is %d", i, j );
    return 0;
}